---
description: 先需求分析与文档确认，再改代码；区分业务需求与架构调整
alwaysApply: true
---

# 需求先行：分析 → 确认 → 文档 → 开发

用户提出需求、优化或架构调整时，**先分析与落文档，需求方确认后再改代码**。禁止带着未决问题直接实现。

## 流程

1. **分类**：业务需求 / 架构调整 / 二者兼有。不明确则先问需求方。
2. **分析**：对照 `docs/` 已有文档，列出目标、范围、不做什么、约束、验收标准、待确认项。
3. **待确认即停**：有不明确或待定项时，只提问、等确认；**不写实现代码**。
4. **落文档后再开发**：
   - **新的独立需求**：新建对应需求文档。
   - **原有需求的修改/优化**：先改对应文档，再改代码。
   - **架构方向调整**：先更新或新增架构文档，再改代码。
5. 开发范围以已确认文档为准；文档未写的能力不做。

## 文档位置

| 类型 | 路径 |
| --- | --- |
| 产品总 FRD | `docs/functional-requirements.md` |
| 独立业务需求 | `docs/requirements/<kebab-slug>.md` |
| 架构 / 实现总览 | `docs/implementation-summary.md` |
| 独立架构决策 | `docs/architecture/<kebab-slug>.md` |

独立需求文档与总 FRD / 总览互相链接。新文档须含：背景与目标、范围（含明确不做）、待确认项（确认后删除或改为已决）、验收标准。架构文档另写决策、影响面（api / mobile / shared / 数据）、备选方案。

## 例外

仅当需求方明确说「跳过文档 / 直接改代码」，或改动仅为笔误、格式、与行为无关的注释时，可跳过本流程。
