# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

手机端 UI 使用 Tamagui。改界面前必须读仓库 `.cursor/skills/tamagui/SKILL.md`，并检索 `.cursor/skills/tamagui/llms.txt`（官方 https://tamagui.dev/llms.txt）。

业务与 UI 必须同时兼容 iOS 与 Android。

仓库级约定与质量门禁见根目录 `AGENTS.md`。
