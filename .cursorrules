# NativeLocation — Cursor Rules

约定以根目录 `AGENTS.md` 与 `.cursor/rules/` 为准。

- 需求先行：`.cursor/rules/requirements-first.mdc`
- Git 提交说明：`.cursor/rules/git-commit.mdc`（简体中文 Conventional Commits）
- 双端：`.cursor/rules/platform-ios-android.mdc`
- 手机端 UI：`.cursor/rules/tamagui.mdc`，组件 API 以 `.cursor/skills/tamagui/llms.txt` 为准
- 质量门禁：`pnpm format`、`pnpm lint:fix`、`pnpm typecheck`（或 `pnpm precommit`）
- 仅在用户明确要求时执行 `git commit` / `git push`
